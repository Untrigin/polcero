---
import Layout from '../../layouts/Layout.astro';
import PageHeader from '../PageHeader.astro';
import ProductCard from '../ProductCard.astro';
import Reveal from '../Reveal.astro';
import { getContent } from '../../data/content';
import { localizedPath, type Locale } from '../../lib/i18n';

interface Props { locale: Locale }
const { locale } = Astro.props;
const c = getContent(locale);
const p = (path: string) => localizedPath(path, locale);
const t = locale === 'pl'
  ? { home: 'Start', products: 'Produkty', lead: 'Jedna platforma w wielu konfiguracjach. Wspólny rdzeń, wymienne końcówki robocze i różne programy dla różnych zadań.', order: 'Zobacz produkt', title: 'Produkty' }
  : { home: 'Home', products: 'Products', lead: 'One platform in many configurations. A shared core, swappable end-effectors and different programs for different tasks.', order: 'View product', title: 'Products' };

const crumbs = [
  { label: t.home, href: p('/') },
  { label: t.products, href: p('/products/') },
];
---
<Layout title={`${t.products} - POLCERO`} description={t.lead.slice(0, 154)} canonicalPath={p('/products/')}>
  <PageHeader eyebrow="POLCERO" title={t.title} lead={t.lead} crumbs={crumbs} />
  <section class="section">
    <div class="container-x">
      <div class="grid">
        {c.products.map((product, i) => (
          <Reveal delay={i * 50}>
            <ProductCard product={product} locale={locale} ctaLabel={t.order} />
          </Reveal>
        ))}
      </div>
    </div>
  </section>
</Layout>

<style>
  .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: clamp(1.5rem, 2.5vw, 2.5rem); }
  @media (max-width: 900px) { .grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 560px) { .grid { grid-template-columns: 1fr; } }
</style>
